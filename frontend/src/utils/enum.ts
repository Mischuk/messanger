export enum FieldsError {
    Name = 'name',
    Password = 'password',
}

export enum StorageKeys {
    Token = 'jwtToken',
}

export enum Routes {
    Auth = 'auth',
    Settings = 'settings',
    Messanger = '/',
}
